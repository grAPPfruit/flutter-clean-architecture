name: flutter_architecture
description: A Flutter application about architecture.
version: 1.0.0+1

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  get_it: ^3.0.3
  rxdart: ^0.22.4
  states_rebuilder: ^1.6.1
  streaming_shared_preferences: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
