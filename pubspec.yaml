name: flutter_architecture
description: A Flutter application about architecture.
version: 1.0.0+1

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  fimber: ^0.2.0
  get_it: ^1.0.3+1
  get_version: ^0.1.0
  rxdart: ^0.22.0
  streaming_shared_preferences: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
