name: flutter_architecture
description: A Flutter application about architecture.
version: 1.0.0+1

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  bloc: ^1.0.0
  flutter_bloc: ^1.0.0
  get_it: ^3.0.3
  rxdart: ^0.22.4
  sealed_flutter_bloc: ^1.0.0
  sealed_unions: ^3.0.2+2
  streaming_shared_preferences: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
